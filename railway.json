{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm --filter backend exec prisma migrate deploy && pnpm --filter backend run build",
    "nodeVersion": "22"
  },
  "deploy": {
    "startCommand": "node dist/main.js",
    "healthcheckPath": "/api/health",
    "restartPolicyType": "ON_FAILURE"
  }
}
