{
  "buildCommand": "pnpm --filter @sistema-monitoreo/shared-contracts run build && pnpm --filter frontend run build && cp -r apps/frontend/dist .",
  "installCommand": "pnpm install",
  "framework": "vite",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
